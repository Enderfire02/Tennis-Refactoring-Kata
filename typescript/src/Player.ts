// Classe à évaluer

// Class representing the player
class Player {
    // Name and points of the player
    name: string;
    points: number = 0;
  
    // Constructor that initializes the player with his name
    constructor(name: string) {
      this.name = name;
    }
  }